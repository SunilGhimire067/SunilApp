apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginx
  labels:
    app: nginx
spec:
  selector:
    app: nginx
  ports:
    - nodePort: 32742
      protocol: TCP
      port: 8080
      targetPort: 80
  type: NodePort